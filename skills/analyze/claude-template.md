<!--
  stroi scope CLAUDE.md — the single per-scope file. It holds two regions:

    1. HAND-MAINTAINED rules (top) + "## Relevant Skills" + "## Code Review" (bottom).
       Yours to edit. /stroi:analyze and /stroi:learn never overwrite these.
    2. The stroi MAP BLOCK between the <!-- stroi:map:start --> / <!-- stroi:map:end -->
       markers — an agent-optimized technical description of this scope, generated and
       refreshed by /stroi:analyze. Everything BETWEEN the markers is overwritten on refresh.

  The map DESCRIBES the code; the rules region holds the RULES. Keep them in their regions.

  Hierarchy (DRY): the ROOT CLAUDE.md map holds only codebase-wide facts and is always loaded;
  a LEAF CLAUDE.md map (per app/package) holds ONLY folder-specific facts and never repeats the
  root. Budget for the map block: root < ~150 lines, leaf < ~400. High signal only — not an
  exhaustive API dump.
-->
# <scope name>

<!-- Hand-maintained rules / conventions you most want enforced here (a pilot's checklist).
     /stroi:learn appends earned rules under "## Conventions (earned)". Keep this lean. -->
## Conventions (earned)
- <rule — what + why>

<!-- stroi:map — generated by /stroi:analyze. Content BETWEEN the markers below is overwritten
     on refresh; edit it only by re-running the command. The rules above and the Relevant Skills
     / Code Review sections below the end marker are yours and are preserved across refreshes. -->
<!-- stroi:map:start -->
last-synced: <commit-short-hash and/or ISO date — from git, never invented>

## Purpose
<1–2 lines: what this scope is for.>

## Entry Points
- <file:line — mains, route files, exported modules>

## Key Directories
- `dir/` — <what lives here, why it matters>

## Core Modules & Responsibilities
- `path` — <responsibility>

## Data Flow / Critical Paths
<how a request/action moves through this scope; only the few paths worth knowing.>

## Conventions (scope-specific)
<naming, error handling, state, testing — only what is specific to this scope, each with a path.>

## Integration Points
<APIs, databases, queues, sibling packages this scope talks to.>

## Dependencies & Docs
<!-- Key libraries for this scope → Context7 id (preferred) and/or doc URL. Used for
     research-before-implement by the planner/developer. Refreshed by /stroi:analyze. -->
- `<lib>@<version>` → ctx7: `</org/lib>`  (docs: <url>)

## Commands
- build: <cmd>
- typecheck: <cmd>
- lint: <cmd>
- test: <cmd>

## Gotchas / Landmines
- <anything surprising or easy to break>
<!-- stroi:map:end -->

## Relevant Skills
<!-- MAINTAINED BY HAND (no command). One installed skill ID per line. These are applied
     automatically during development, verification, and review for THIS scope only — because
     this CLAUDE.md loads just-in-time when you work here. Examples:
       - my-react-native-checks
       - everything-claude-code:react-native-patterns
     Leave empty if none. /stroi:analyze never touches anything outside the map markers. -->

## Code Review
<!-- MAINTAINED BY HAND. Prose: what specifically matters when reviewing code in this scope,
     beyond the universal correctness/security checks the reviewer always runs. Preserved
     across refreshes (it lives outside the map markers). -->
