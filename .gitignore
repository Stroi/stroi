# OS / editor
.DS_Store
*.swp

# Node (only present if a hook/tool ever needs deps; none bundled today)
node_modules/

# stroi runtime artifacts (plans, RESUME, dirty.log) — written into a *consuming*
# project under .claude/stroi/, never committed; ignored here too in case the harness
# is exercised against this repo itself.
.claude/stroi/
