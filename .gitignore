# OS / editor
.DS_Store
*.swp

# Node (only present if a hook/tool ever needs deps; none bundled today)
node_modules/

# stroi runtime (plans, RESUME, dirty.log) now lives OUTSIDE the repo, under
# ~/.claude/stroi/<dashified-project-path>/ — so nothing needs ignoring here anymore.
# This entry is kept defensively to catch artifacts from older stroi versions or a manual run.
.claude/stroi/
